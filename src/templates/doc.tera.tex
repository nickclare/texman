\documentclass{%- if metadata.document_options is defined -%}[{{ metadata.document_options | join(sep=',') }}]{%- endif -%}{{'{' ~ metadata.document_class ~ '}'}}
\usepackage{import}

{% for pr in metadata.prelude_includes -%}
\import{{ '{' ~ prelude_root ~ '}{' ~ pr ~ '}'}}
{% endfor -%}


\begin{document}
{% for sec in metadata.sections -%}
\import{{ '{' ~ document_root ~ '}{' ~ sec ~ '}'}}
{% endfor -%}
\end{document}